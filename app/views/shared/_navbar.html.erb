<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
   <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">

      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to translate("home"), root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to translate("all_posts"), posts_path, class: "nav-link" %>
        </li>
      </ul>
        <% if user_signed_in? %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to translate("my_posts"), user_post_path(current_user), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to translate("create_post"), new_post_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to translate("unapprove_posts"), unapprove_posts_path, class: "nav-link" %>
            </li>
          </ul>
          <%= render partial: "shared/language" %>
        <% end %>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
           <%= link_to current_user.username, edit_user_registration_path, class: "nav-link" %> 
          </li>
          <li class="nav-item">
           <%= link_to translate("profile"), profile_path(current_user), class: "nav-link" %> 
          </li>
          <li class="nav-item">
          <%= button_to translate("logout"), destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
          </li>
        <% else %>
          <li class="nav-item">
          <%= link_to translate("sign_up"), new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
          <%= link_to translate("login"), new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
